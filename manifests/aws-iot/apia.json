{
  "apia": "1.0",
  "service": {
    "id": "aws-iot",
    "name": "AWS IoT Core API",
    "description_for_ai": "Managed IoT platform by Amazon. Connect billions of devices, process and route messages, integrate with AWS services. MQTT and REST APIs. Enterprise-grade IoT infrastructure.",
    "url": "https://aws.amazon.com/iot-core",
    "api_base": "https://{endpoint}.iot.{region}.amazonaws.com",
    "category": "iot",
    "geo": [
      "GLOBAL"
    ],
    "language": "en",
    "docs": "https://docs.aws.amazon.com/iot/latest/developerguide/iot-device-shadows.html"
  },
  "auth": {
    "type": "aws_sig_v4",
    "anonymous_access": false,
    "how_to_get": "AWS account at https://aws.amazon.com",
    "cost": "Free: 250K messages/month. Then $1/million messages"
  },
  "capabilities": [
    {
      "id": "update_shadow",
      "description_for_ai": "Update device shadow (desired and reported state). Device shadows persist state even when device offline.",
      "intent": [
        "update IoT device",
        "set device state",
        "device shadow",
        "IoT state management"
      ],
      "endpoint": "POST https://{endpoint}.iot.{region}.amazonaws.com/things/{thing_name}/shadow",
      "input": {
        "thing_name": {
          "type": "string",
          "required": true
        },
        "state": {
          "type": "object",
          "description": "{desired: {key: value}} or {reported: {key: value}}",
          "required": true
        }
      },
      "output": {
        "type": "shadow",
        "fields": [
          "state",
          "metadata",
          "version",
          "timestamp"
        ]
      },
      "realtime": true,
      "requires_auth": true
    },
    {
      "id": "publish_message",
      "description_for_ai": "Publish a message to an MQTT topic via REST. Subscribed IoT devices receive it immediately. Use for sending commands to devices.",
      "intent": [
        "send IoT message",
        "MQTT publish",
        "IoT command",
        "device message"
      ],
      "endpoint": "POST https://{endpoint}.iot.{region}.amazonaws.com/topics/{topic}",
      "input": {
        "topic": {
          "type": "string",
          "description": "MQTT topic e.g. devices/sensor1/data",
          "required": true
        },
        "payload": {
          "type": "object",
          "description": "Message payload",
          "required": true
        }
      },
      "output": {
        "type": "empty",
        "fields": []
      },
      "realtime": true,
      "requires_auth": true
    }
  ],
  "meta": {
    "apia_version": "1.0",
    "manifest_author": "APIA community",
    "last_verified": "2026-06-14"
  },
  "agent_hints": {
    "endpoint": "Get your IoT endpoint: aws iot describe-endpoint --endpoint-type iot:Data-ATS",
    "auth": "Use IoT certificates (X.509) for devices, IAM for API calls. Policy must allow iot:Publish action.",
    "mqtt": "Prefer MQTT SDK over REST for high-frequency device data. REST is for one-off operations.",
    "shadow": "Device Shadow stores last known state. Useful when device is offline — state syncs on reconnect."
  }
}